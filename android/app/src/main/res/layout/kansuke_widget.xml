<?xml version="1.0" encoding="utf-8"?>
<!--
  Issue #127: ホーム画面ウィジェットの外枠（FR-4 の「開かずに予定が見える」）。
  一覧は KanSukeWidgetService が供給する（RemoteViews は動的に子を足せないため）。
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/kansuke_widget_background"
    android:orientation="vertical"
    android:padding="12dp">

    <!-- タップでアプリを開く（月表示へ）。 -->
    <LinearLayout
        android:id="@+id/widget_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingBottom="6dp">

        <ImageView
            android:layout_width="18dp"
            android:layout_height="18dp"
            android:contentDescription="@null"
            android:src="@mipmap/ic_launcher" />

        <TextView
            android:id="@+id/widget_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:layout_weight="1"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="@string/kansuke_widget_label"
            android:textColor="@color/widget_text_subtle"
            android:textSize="11sp" />
    </LinearLayout>

    <ListView
        android:id="@+id/widget_list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:divider="@null"
        android:dividerHeight="0dp" />

    <!-- 一覧が空になったとき（サービスに繋がらないなど）の代わりの表示。 -->
    <TextView
        android:id="@+id/widget_empty"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center"
        android:text="@string/kansuke_widget_open_app"
        android:textColor="@color/widget_text_subtle"
        android:textSize="12sp" />
</LinearLayout>
