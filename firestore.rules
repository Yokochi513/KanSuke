rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Security Rules の本実装までは、すべてのアクセスを拒否する。
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
