{
  "name": "kansuke-firestore-rules-tests",
  "private": true,
  "scripts": {
    "lint": "eslint .",
    "test": "firebase emulators:exec --only firestore --project demo-kansuke \"mocha --exit test/**/*.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "eslint": "^9.30.1",
    "firebase": "^11.10.0",
    "firebase-tools": "^14.9.0",
    "mocha": "^11.7.1"
  }
}
